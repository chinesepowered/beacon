/** Per-app identity. The only file that differs between our hackathon repos. */
export const APP_SLUG = "beacon";
/** Prefix for per-case email routing codes, e.g. BCN-7F3K. */
export const CASE_PREFIX = "BCN";
export const APP_NAME = "Beacon";
